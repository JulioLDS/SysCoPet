import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const primary = Color(0xFFFF0F8B8D);
  static const secondary = Color(0xFFFF7ED6C1);
  static const background = Color(0xFFFFF8FBFB);
  static const light = Color(0xFFFFDFF5F0);

  // Cores de texto
  static const textPrimary = Color(0xFFFF1E252B);
  static const textSecondary = Color(0xFFFF5F6B6D);

  // Cores de borda
  static const border = Color(0xFFFFD9E3E3);

  // Cores adicionais (úteis para o app)
  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const white = Colors.white;
  static const black = Colors.black;
}
